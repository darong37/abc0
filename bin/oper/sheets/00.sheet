#!/bin/sh -u

### Sheet
cat >> ${stxt} <<EOF

######################################################################
# 00. ${host} ‚É ${user} ‚ÅƒƒOƒCƒ“
######################################################################
$ whoami ; id ; uname -n 
	${user}
	{*}(${user}) {*}
	${host}

$ pwd
	${homedr}


EOF
