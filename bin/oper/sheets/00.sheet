#!/bin/sh -u

### Sheet
cat >> ${stxt} <<EOF

######################################################################
# 00. ${host} ‚É ${user} ‚ÅƒƒOƒCƒ“
######################################################################
$ $( if [[ $user != 'root' ]];then echo "su - ${user}"; fi )

$ whoami ; id ; uname -n 
	${user}
	uid=1008(${user}) gid=105(dba) groups=1(staff)
	${host}
$

EOF
