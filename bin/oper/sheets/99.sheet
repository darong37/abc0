#!/bin/sh -u

### Sheet
cat >> ${stxt} <<EOF

######################################################################
# 99.‚¨–ñ‘©
######################################################################
$ whoami ; id ; uname -n
	${user}
	uid=1008(${user}) gid=105(dba) groups=1(staff)
	${host}
$
$ exit

EOF
