#!/bin/sh -u

### Sheet
cat >> ${stxt} <<EOF

######################################################################
# 99.お約束
######################################################################
$ export PS1='% '

$ date

$ jobs
	{(\s*)}
	#! バックグランドプロセスがないことを確認

$ whoami ; id ; uname -n
	${user}
	{*}(${user}) {*}
	${host}

$ exit

EOF
