#!/bin/sh -u

homedr=/u45/e058/orae058

orasid='e058'
Echo "orasid:'${orasid}'"

### Sheet
. $APPLDIR/sheets/00.sheet

usedb=${usedb:-$( Ask 'Do you want to use DB ?' )}
if [[ $usedb = 'y' ]];then
  lsnr=e058
  oraver=11g
  orahom=/u45/e058/oracle/db/tech_st/11.2.0
  oradat=/u45/e058/oracle/db/apps_st/data/e058
  alertlog=/u45/e058/oracle/diag/rdbms/e058/e058/trace/alert_e058.log
  cat >> ${stxt} <<EOS
$ . $orasid.env

$ echo \$ORACLE_SID
	$orasid

$ echo \$ORACLE_HOME
	$orahom

EOS
fi

